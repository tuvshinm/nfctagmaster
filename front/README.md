# Front. React + TypeScript + Vite

`cd front`
`npm i` or `bun i`
`npm run dev`

# Backend. Python + PostgreSQL + NFCpy

`cd back`
`python -m venv venv`
`venv\Scripts\Activate.ps1`
`pip install -r requirements.txt`
`fastapi dev main.py`
